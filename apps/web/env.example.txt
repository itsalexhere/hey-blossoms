# ============================================================
# Supabase Configuration
# Copy this file to .env.local and fill in your values.
# Get these from: Supabase Dashboard > Project Settings > API
# ============================================================

# Public Project URL (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co

# Public anon key (safe to expose to browser — used for public reads)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (SERVER ONLY — never expose to browser!)
# Used by the scraper and admin server actions to bypass RLS for writes.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
